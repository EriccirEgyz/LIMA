# IDE & Editor Files
.DS_Store
.cursor/
.vscode/

# Python
__pycache__/
*.egg-info/

# Environment & Secrets
.env

# Logs
*.log
/log*/

# Data & Checkpoints
/data*/
/checkpoint/
/models/

# Evaluation Outputs (BFCL, tau2-bench, etc. — large, regenerable)
/results/

# Libraries & Dependencies
/lib/

# Unmodified vendored eval clones — re-cloned by setup_*.sh (not committed)
/src/eval/gorilla/
/src/eval/tau2-bench/

# OmniaBench IS committed as source (modified for this repro):
#   - deepseek thinking 控制 patch(agent开/user关/judge关,3处注入,2026-08-27)
#   - 上游基线: github.com/scuuy/OmniaBench @ 6f91930(其 .git 已移除,更新需手动 diff)
# 其 .venv / __pycache__ 由下方全局规则排除

# Backup / one-off patch files (keep in working tree, not in history)
*.bak-*
*.fixA-*

# LLaMA-Factory & verl ARE committed as source (modified for this repro),
# but their weights / generated data / run artifacts are excluded:
LLaMA-Factory/models/
verl/data/env_factory_rl.json
**/log_rollout/
**/outputs/
**/tensorboard_log/
**/checkpoints/
*.safetensors
*.bin
*.pt
*.pth

# Virtualenvs & caches (never commit)
.venv/
.venv-sft/
.cache/

# Databases
*.db

# Serialized/Cache Files
*.pkl